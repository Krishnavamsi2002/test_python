name: Print Hello

on:
  workflow_dispatch:

jobs:
  Print-Hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello world"
      - name: run python code
        uses: actions/checkout@v3
      - name: install python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: run function main.py
        run: pyhon main.py
        
          
  
